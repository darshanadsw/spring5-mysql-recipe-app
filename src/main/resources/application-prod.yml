spring:
  datasource:
    url: jdbc:mysql://${mysql.host}:3306/dw_prod
    username: ${mysql.user}
    password: ${mysql.pass}
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false